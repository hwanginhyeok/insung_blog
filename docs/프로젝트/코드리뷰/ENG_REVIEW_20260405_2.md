# 엔지니어링 코드 리뷰 — 2026-04-05 (2차)

> 대상: `origin/master~20..origin/master` (20개 커밋)
> 리뷰어: Claude Opus 4.6
> 기준: 아키텍처 / 안전성 / 성능 / 타입 안전성 / 코드 품질 / 테스트

---

## 커밋별 판정

### 1. `9515d9f` — fix(web): 로그인 페이지 기본 모드를 로그인으로 변경

| 기준 | 판정 |
|------|------|
| 아키텍처 | PASS |
| 안전성 | PASS |
| 코드 품질 | PASS |

- 2파일, 9줄 변경. `?mode=signup` 제거하여 항상 로그인 먼저 표시
- useSearchParams 의존성이 사라져 코드가 단순해짐
- **판정: PASS**

---

### 2. `25c38aa` — perf(timing): 블로거 대기 30-60초 → 20-40초 + 정규 분포

| 기준 | 판정 |
|------|------|
| 아키텍처 | PASS |
| 안전성 | CONCERN |
| 성능 | PASS |

- `random.uniform` → `random.gauss` 변경으로 자연스러운 분포 달성
- **CONCERN**: `max()` 하한만 있고 상한 클램프 없음. `gauss`는 이론적으로 무한 상한이므로 극단적 아웃라이어(60초+)가 나올 수 있음. `min(DELAY_MAX * 1.5, gauss())` 같은 상한 클램프 권장
- **판정: CONCERN** — 상한 클램프 부재

---

### 3. `faeb2c1` — fix(tier): 플랜별 AI 글쓰기 한도 현실화

| 기준 | 판정 |
|------|------|
| 타입 안전성 | PASS |
| 코드 품질 | PASS |

- TIER_LIMITS 타입에 `price` 필드 추가. 9999를 "무제한" 매직 넘버로 사용
- **판정: PASS** — 매직 넘버 9999는 UI에서 "무제한" 분기 처리가 되어 있어 괜찮음

---

### 4. `6bad90d` — feat(billing): 카카오페이 정기결제 기능 추가 (포트원 V2)

| 기준 | 판정 |
|------|------|
| 아키텍처 | CONCERN |
| 안전성 | ISSUE |
| 성능 | PASS |
| 타입 안전성 | CONCERN |
| 테스트 | PASS |

**가장 큰 커밋 (23개 파일). 결제 시스템이므로 엄격하게 평가.**

**ISSUE — subscribe 최초 결제 → 스케줄 등록 사이 실패 시 롤백 없음 (subscribe/route.ts)**
- 3단계 (즉시결제) 성공 후 4단계 (스케줄 등록) 실패 시 돈은 빠져나갔지만 구독이 등록되지 않는 상태가 됨
- 5단계 (DB 업데이트) 실패 시에도 마찬가지
- 보상 트랜잭션(결제 취소) 또는 최소한 결제 이력 기록 필요

**ISSUE — 웹훅에서 customData 의존 (webhook/route.ts)**
- `Transaction.Paid` 이벤트에서 `customData`로 userId/tier를 파싱하는데, subscribe에서 `customData`를 `payWithBillingKey`에 전달하지 않음
- 결과적으로 웹훅이 수신되어도 userId가 undefined가 되어 tier 업그레이드가 실패함
- `payWithBillingKey` 함수에 customData 파라미터 추가 필요

**CONCERN — timing attack 가능성 (verifyWebhookSignature)**
- `expected === signature` 단순 비교는 타이밍 사이드채널 공격에 취약
- `crypto.timingSafeEqual` 사용 권장

**CONCERN — CRON_SECRET이 Bearer 토큰으로만 보호 (expire-subscriptions)**
- 보안 자체는 OK이지만, CRON_SECRET 미설정 시 서비스가 바로 실패하는 대신 `authHeader !== \`Bearer ${undefined}\`` 비교가 됨
- 명시적 체크 추가 권장: `if (!process.env.CRON_SECRET) return 500`

**CONCERN — 마이그레이션 파일 중복**
- `00026_add_subscription_fields.sql`과 `20260405200000_add_subscription_fields.sql`이 같은 내용으로 두 곳에 존재
- 하나의 네이밍 컨벤션으로 통일 필요

**양호한 점:**
- 테스트 5개 파일 (webhook 멱등성, cron 만료, subscribe/cancel/sync) — 핵심 시나리오 커버
- portone.ts 래퍼가 깔끔하게 분리됨
- billing 페이지 UX 완성도 높음 (상태 표시, 해지/동기화)

- **판정: ISSUE** — 결제 롤백 부재 + customData 미전달 심각

---

### 5. `2030ec6` — docs: 세션 작업 문서화

- 문서 변경만. 코드 리뷰 대상 아님
- **판정: PASS**

---

### 6. `3efd1b7` — feat(ops): 사용자 임계점 15명/20명 알림 추가

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- 기존 마일스톤 배열에 15/20 추가, 메시지에 참조 문서 경로 포함
- 잘 구조화된 변경
- **판정: PASS**

---

### 7. `a99426c` — feat(tier): 댓글/이웃봇 플랜별 한도 적용

| 기준 | 판정 |
|------|------|
| 아키텍처 | CONCERN |
| 안전성 | PASS |
| 타입 안전성 | PASS |

**CONCERN — 한도 정의 중복 (tier.ts vs supabase_client.py)**
- TypeScript 쪽 `tier.ts`에 `{ commentsPerDay: 10, bloggersPerDay: 3, ... }`
- Python 쪽 `supabase_client.py`에 `_TIER_LIMITS = { "free": {"comments": 10, ...} }`
- 두 곳에서 같은 값을 독립적으로 정의하고 있어, 한 쪽만 수정하면 불일치 발생
- 해결: DB에 tier_limits 테이블을 두거나, 최소한 둘 다 참조하는 단일 소스(config.json 등) 필요

**양호한 점:**
- DB의 `max_comments_per_day` 하드코딩을 tier 기반으로 오버라이드하는 설계 자체는 올바름
- tier 조회 실패 시 `"free"` 폴백 처리됨

- **판정: CONCERN** — 한도 정의 이중화

---

### 8. `dd68616` — fix(web): 랜딩 페이지 문구 플랜/기능 반영

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- 문구를 실제 한도에 맞게 업데이트 (10→5회, 30→100개 등)
- **판정: PASS**

---

### 9. `c9f63d9` — fix(web): 랜딩 사용법 스텝 실제 플로우 반영

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- 3단계→4단계로 확장. 실제 사용 흐름과 일치
- **판정: PASS**

---

### 10. `fc19e9b` — feat(ux): 봇 명령 완료/실패 시 팝업 알림 + 알림음

| 기준 | 판정 |
|------|------|
| 아키텍처 | CONCERN |
| 안전성 | PASS |
| 성능 | CONCERN |

**CONCERN — useBotStatus 훅에 너무 많은 책임**
- 데이터 fetch, 폴링, 탭 가시성, 알림음, 브라우저 Notification, 토스트까지 한 훅에 집중
- 알림 관련 로직을 별도 `useCommandNotification` 훅으로 분리 권장

**CONCERN — AudioContext 누수 가능성**
- `new AudioContext()`를 매 알림마다 새로 생성하지만 close하지 않음
- 브라우저마다 AudioContext 개수 제한이 있음 (보통 6~8개)
- 싱글톤 AudioContext 사용 또는 사용 후 `ctx.close()` 호출 필요

**양호한 점:**
- 첫 로드 시 이전 완료 건 무시 로직 올바름
- Web Audio API try/catch 처리됨
- Notification 권한 요청 타이밍 적절

- **판정: CONCERN** — AudioContext 누수 + 훅 과부하

---

### 11. `2b39c0b` — style(design): FINDING-001~029 디자인 시스템 토큰 통일

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |
| 아키텍처 | PASS |

- 14개 파일에서 하드코딩 색상→디자인 토큰 교체
- login의 `startsWith("✓")` 해키 패턴을 `successMessage` 상태로 분리 — 올바른 리팩토링
- **판정: PASS** — 디자인 시스템 일관성 크게 향상

---

### 12. `a814b48` — style(design): FINDING-030~035 봇 컴포넌트 색상 토큰 통일

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- 4개 봇 컴포넌트의 `text-red-500` → `text-destructive`, `bg-green-500` → `bg-primary` 등
- **판정: PASS**

---

### 13. `cf5a8f1` — docs(design): 전체 사이트 디자인 리뷰 리포트 작성

- 문서 전용. 328줄의 상세한 디자인 리뷰 리포트
- **판정: PASS**

---

### 14. `46787b6` — feat(ux): 토스트 알림 + 에러 페이지 + 프로필 + 다크모드 + 검색

| 기준 | 판정 |
|------|------|
| 아키텍처 | CONCERN |
| 안전성 | PASS |
| 성능 | PASS |
| 타입 안전성 | PASS |
| 코드 품질 | CONCERN |

**CONCERN — 커밋 범위가 너무 넓음**
- 5가지 독립 기능이 한 커밋에 들어감. 롤백 시 다른 기능까지 함께 롤백됨
- 각 기능별 커밋이 바람직했음

**CONCERN — 프로필 페이지에서 한도 재정의 (profile/page.tsx)**
- `TIER_MAX` 객체로 한도를 다시 정의: `{ free: 5, basic: 30, pro: 9999 }`
- tier.ts의 TIER_LIMITS에서 import해야 하는데, "use client"라서 서버 전용 코드를 사용 못 하는 제약이 있음
- 해결: tier.ts에서 클라이언트에서도 쓸 수 있는 상수를 export하거나, 별도 `tier-constants.ts` 분리

**양호한 점:**
- Toaster 구현이 Context API로 깔끔
- 404/500 에러 페이지 적절 (개발모드에서만 에러 상세 표시)
- 검색 debounce 300ms 적절
- 에러 페이지의 "홈으로" 링크가 `/write`를 가리키는 것은 의도적이나 약간 혼란스러울 수 있음

- **판정: CONCERN** — 커밋 범위 과대 + 한도 상수 중복

---

### 15. `5935e5b` — feat(ux): 플랜 비교표 + 도움말 + 쿠키 가이드 + 월간 리포트 + 버전 비교

| 기준 | 판정 |
|------|------|
| 아키텍처 | CONCERN |
| 성능 | CONCERN |
| 코드 품질 | CONCERN |

**CONCERN — 커밋 범위가 너무 넓음 (5가지 기능)**
- 14번과 동일 문제

**CONCERN — 버전 diff 알고리즘이 naive**
- `write/page.tsx`의 줄 단위 diff가 단순 인덱스 비교 (oldLines[i] vs newLines[i])
- 줄이 삽입/삭제되면 이후 모든 줄이 "change"로 표시됨
- LCS(최장 공통 부분 수열) 기반 diff 또는 `diff` 라이브러리 사용 권장

**CONCERN — MonthlyReport에서 runs가 변경될 때마다 전체 재계산**
- `useEffect([runs])`로 매번 filter + reduce. runs가 클 경우(수백 건) 부하
- `useMemo` 사용 권장

**양호한 점:**
- 플랜 비교표 데이터가 `COMPARISON_ROWS` 배열로 깔끔하게 분리
- HelpTooltip 외부 클릭 닫기 로직 올바름
- 쿠키 가이드 4단계 UI가 직관적

- **판정: CONCERN** — diff 알고리즘 한계 + 커밋 범위

---

### 16. `b64db54` — revert(ux): 다크모드 토글 제거

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- "반쯤 되는 다크모드는 안 하는 것보다 나쁨" — 올바른 결정
- theme-toggle.tsx 삭제 + FOUC 방지 스크립트 제거
- 단, 토스트 컴포넌트(toast.tsx)에 `dark:` 클래스가 여전히 남아있음 (해 없지만 dead code)
- **판정: PASS**

---

### 17. `7a7bdf8` — fix(web): 헤더 프로필 링크 → "마이페이지" 명시

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- 이메일 앞부분 표시 → "마이페이지" 고정 텍스트. UX 명확성 향상
- **판정: PASS**

---

### 18. `d52f079` — fix(web): 댓글 승인 패널 스크롤 영역 추가

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- 대기 댓글 `max-h-[500px]`, 승인 댓글 `max-h-[300px]` 스크롤
- **판정: PASS**

---

### 19. `03c1e35` — fix(web): 최근 교류 접기/펼치기 + 스크롤 추가

| 기준 | 판정 |
|------|------|
| 코드 품질 | PASS |

- 기본 접힘 + 클릭 펼침 + max-h-400px 스크롤
- 색상 토큰화(gray→muted, blue/green→primary) 동시 적용
- reply_sent 교류 타입 추가
- **판정: PASS**

---

### 20. `7f18236` — feat(web): 이웃봇에 댓글 승인/게시/자동화 기능 추가

| 기준 | 판정 |
|------|------|
| 아키텍처 | CONCERN |
| 안전성 | CONCERN |
| 코드 품질 | CONCERN |

**CONCERN — neighbor/page.tsx가 비대해짐**
- 이 커밋 후 page.tsx가 약 700줄 이상. 상태 16개+, 함수 10개+
- 댓글 승인/거부/게시 로직이 bot 페이지의 CommentApprovalPanel과 유사하지만 인라인으로 작성됨
- 공통 승인 로직을 hook(`useCommentApproval`)으로 추출 권장

**CONCERN — 게시 완료 폴링 타임아웃 처리 미흡 (handleExecute)**
- `setInterval(pollStatus, 3000)` + `setTimeout(clearInterval, 300000)` 조합
- 5분 타임아웃 후 `setExecuting(false)`만 하고 사용자에게 알림 없음
- 타임아웃 시 "시간이 초과되었습니다. 결과를 확인해주세요." 메시지 필요

**CONCERN — 자동 승인+자동 게시 경고 메시지 위치**
- 두 토글 모두 켜졌을 때 `text-yellow-600` 경고가 있지만, 디자인 토큰이 아닌 하드코딩 색상
- FINDING-001~029에서 토큰화했는데 이 커밋에서 다시 하드코딩 발생

**양호한 점:**
- bot-api.ts 함수를 직접 import하여 중복 코드 없이 재사용 (설계 원칙 준수)
- 승인 취소(revoke) 기능 포함 — UX 완성도 높음
- 자동 승인+자동 게시 동시 켜짐 경고 표시

- **판정: CONCERN** — 컴포넌트 비대화 + 타임아웃 UX 미흡

---

## 전체 요약

### 판정 분포

| 판정 | 건수 | 커밋 |
|------|------|------|
| **PASS** | 12 | 9515d9f, faeb2c1, 2030ec6, 3efd1b7, dd68616, c9f63d9, 2b39c0b, a814b48, cf5a8f1, b64db54, 7a7bdf8, d52f079 |
| **CONCERN** | 7 | 25c38aa, a99426c, fc19e9b, 46787b6, 5935e5b, 03c1e35, 7f18236 |
| **ISSUE** | 1 | 6bad90d |

### 총평

전체적으로 기능 구현 속도와 커버리지가 인상적이다. 특히 디자인 시스템 토큰 통일(29건+6건)은 코드베이스 일관성을 크게 높였고, 결제 시스템 테스트가 핵심 시나리오를 잘 커버한다.

그러나 결제(billing) 파이프라인에 **결제 성공 후 스케줄 등록/DB 업데이트 실패 시 보상 트랜잭션이 없는 심각한 안전성 이슈**가 있다. 이것은 실제 돈이 관련된 문제이므로 프로덕션 배포 전 반드시 수정해야 한다.

---

## 개선 권장사항 (우선순위순)

### P0 — 프로덕션 배포 차단

1. **결제 롤백 메커니즘 추가** (`subscribe/route.ts`)
   - 스케줄 등록 실패 시 즉시결제 환불 처리
   - DB 업데이트 실패 시 최소한 결제 이력은 기록하여 수동 복구 가능하도록
   - 또는 Saga 패턴: 즉시결제 → 성공 기록 → 스케줄 등록 → DB 업데이트, 각 단계 실패 시 이전 단계 보상

2. **customData 전달 누락 수정** (`portone.ts` + `subscribe/route.ts`)
   - `payWithBillingKey()`에 customData 파라미터 추가
   - subscribe에서 `JSON.stringify({ userId: user.id, tier })` 전달
   - 웹훅이 customData에 의존하므로 이 없이는 정기결제 갱신 시 tier 업그레이드 불가

### P1 — 조기 수정 권장

3. **웹훅 서명 검증에 timingSafeEqual 사용** (`portone.ts`)
   ```typescript
   import { timingSafeEqual } from "crypto";
   const a = Buffer.from(expected);
   const b = Buffer.from(signature);
   return a.length === b.length && timingSafeEqual(a, b);
   ```

4. **한도 정의 Single Source of Truth 확보**
   - `tier.ts`와 `supabase_client.py`에서 동일 값이 독립 정의됨
   - 방안 A: Supabase에 `tier_limits` 테이블 생성, 양쪽에서 조회
   - 방안 B: JSON 파일을 한 곳에 두고 빌드 시 양쪽에서 로드

5. **AudioContext 싱글톤** (`useBotStatus.ts`)
   - 모듈 레벨에서 `let audioCtx: AudioContext | null = null` 관리
   - `audioCtx ??= new AudioContext()` 패턴으로 재사용

6. **gauss 분포 상한 클램프** (`delay.py`)
   ```python
   secs = max(MIN * 0.75, min(MAX * 1.5, random.gauss(center, stddev)))
   ```

### P2 — 리팩토링 권장

7. **커밋 단위 세분화** — 특히 UX 관련 커밋(46787b6, 5935e5b)은 5개 기능이 한 커밋에 묶여 있음

8. **neighbor/page.tsx 분리** — `useCommentApproval` 훅 추출 + `AutomationToggles` 컴포넌트 분리

9. **MonthlyReport useMemo 적용**
   ```typescript
   const stats = useMemo(() => { /* 계산 */ }, [runs]);
   ```

10. **버전 diff 알고리즘 교체** — `diff` 라이브러리 도입 또는 LCS 기반 구현

11. **토스트 컴포넌트의 dark: 클래스 정리** — 다크모드 제거 후 불필요해진 dark: prefix 제거

12. **에러 페이지 "홈으로" 링크** — `/write` 대신 `/dashboard` 또는 `/`로 변경 검토

13. **expire-subscriptions CRON_SECRET 미설정 방어**
    ```typescript
    if (!process.env.CRON_SECRET) {
      return NextResponse.json({ error: "CRON_SECRET 미설정" }, { status: 500 });
    }
    ```

---

## 아키텍처 관찰

### 잘 된 점
- **관심사 분리**: portone.ts 래퍼 → API routes → 프론트엔드 3레이어 구조
- **bot-api.ts 재사용**: 이웃봇에서 댓글 승인 API를 중복 없이 import
- **디자인 토큰 일관성**: 35건의 하드코딩 색상을 체계적으로 토큰화
- **테스트 커버리지**: 결제 핵심 시나리오 5개 테스트 — webhook 멱등성, cron 만료 등

### 개선 필요
- **결제 안전성**: 분산 트랜잭션 보상 없음 (P0)
- **상수 중복**: tier 한도가 TS/Python 양쪽에 독립 정의 (P1)
- **컴포넌트 크기**: neighbor/page.tsx ~700줄, useBotStatus ~350줄 — 분리 필요 (P2)
- **커밋 hygiene**: 하나의 커밋에 5개 독립 기능 포함 — 원자적 커밋 원칙 위반 (P2)
