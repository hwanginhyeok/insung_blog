# 프록시 전략 요구사항

> ��성일: 2026-04-05
> 상태: 코드 준비 완��, 실제 프록시 구매는 30명+ 시점

## 배경

모든 사용자의 봇이 서버 IP 1개에서 실행됨.
���일 IP에서 다수 계정 활동 → ���이버 봇 감지 위험.

## 도입 시점

| 사용자 수 | 조치 |
|----------|------|
| ~10명 | 프록시 불필요, 현행 직접 연결 |
| 10~30명 | 프록시 테스트 (1-2 IP로 검증) |
| 30~50명 | 프록시 풀 도입 (20-30 한국 IP) |
| 100명+ | 유저별 ��정 IP 할당 |

## 구현 상태

### 완료 (2026-04-05)
- `src/utils/browser.py`: `create_browser(proxy_url=...)` 파라미터 추가
- `src/storage/supabase_client.py`: `get_user_bot_config()`에서 `proxy_url` 로드
- `supabase/migrations/20260405_add_proxy_url.sql`: bot_settings.proxy_url 컬럼

### 미완료 (30명 도달 �� 진행)
- [ ] command_worker.py 각 핸들러에서 `config["proxy_url"]`을 `create_browser()`에 전달
- [ ] 프록시 업체 선정 및 계약 (Bright Data, SmartProxy 등)
- [ ] 프록시 헬스체크 (주기적 연결 확인)
- [ ] 웹 UI: 관리자 페이지에서 유저별 프록시 할당 관리
- [ ] 프록시 실패 시 직접 연결 폴백

## 프록시 유형

| 유형 | 가격 | 감지 위험 | 추천 |
|------|------|----------|------|
| 데이터센터 | $1-5/IP/월 | 높음 | ❌ |
| 주거용(Residential) | $5-15/GB | 매우 낮음 | ✅ 최적 |
| 모바일 | $20-50/GB | 거의 없음 | ✅ 프리미엄 |
| ISP | $2-5/IP/월 | 낮음 | ✅ 가성비 |

한국 IP 필수. 주거용 추천.

## 비용 예측

| 사용자 수 | �� 프록시 비용 | 유저당 원가 |
|----------|-------------|-----------|
| 50명 | ₩30,000-50,000 | ₩600-1,000 |
| 100명 | ₩100,000-200,000 | ₩1,000-2,000 |
| 1000명 | ₩500,000-1,000,000 | ₩500-1,000 |

프로 플랜 ₩20,000 기준 마진 충분.

## 코드 사용법

```python
# browser.py — proxy_url이 있으면 적용, 없으면 직접 연결
browser, context, page = await create_browser(pw, proxy_url=config.get("proxy_url"))

# bot_settings 테이블
# proxy_url: "http://user:pass@kr-residential.proxy.com:8080"
# proxy_url: NULL  (직접 연결)
```
